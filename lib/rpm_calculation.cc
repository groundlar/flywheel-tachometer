#include "lib/rpm_calculation.h"
#include <vector>

namespace lib_rpm_calculation {

int32_t raw_to_microhertz(const std::vector<uint8_t>& readings, int16_t sample_rate_hz, uint16_t sensor_count) {
    // TODO
    return 0;
}
}